'use client';

import Head from 'next/head';

const page = () => {
    return (
        <div>
            <Head>
                <title>FAQ</title>
            </Head>
            <h1>FAQ</h1>
        </div>
    );
};

export default page;