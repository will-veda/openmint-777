'use client';

import Head from 'next/head';

const page = () => {
    return (
        <div>
            <Head>
                <title>contact us</title>
            </Head>
            <h1>Contact Us</h1>
        </div>
    );
};

export default page;